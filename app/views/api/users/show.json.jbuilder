json.partial! @user, partial: 'user', as: :user 

# json.id user.id
# json.name user.name
# json.email user.name
#add array of kids because there is an association method

#In the kids partial - nest the goal data wtihin the kid data.